name: CI

on:
  push:
    branches: ['**']
  pull_request:
  workflow_dispatch:
  schedule:
    - cron: '17 6 * * 1'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.head_ref || github.ref_name }}
  cancel-in-progress: true

jobs:
  static-and-unit:
    name: Static and unit (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install ShellCheck
        shell: bash
        run: |
          if command -v shellcheck >/dev/null 2>&1; then
            exit 0
          fi
          if [[ "$RUNNER_OS" == "macOS" ]]; then
            brew install shellcheck
          else
            sudo apt-get update
            sudo apt-get install -y shellcheck
          fi

      - name: Validate shell syntax
        run: bash -n bin/speckit-bootstrap install.sh tests/unit.sh tests/smoke-live.sh

      - name: Run ShellCheck
        run: shellcheck bin/speckit-bootstrap install.sh tests/unit.sh tests/smoke-live.sh

      - name: Run isolated unit tests
        run: bash tests/unit.sh

  smoke-pinned:
    name: Pinned upstream smoke (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: '0.10.6'
          enable-cache: false

      - name: Run live bootstrap smoke test
        env:
          SPEC_KIT_VERSION: v0.12.11
          SPECKIT_GITHUB_ISSUE_CANON_VERSION: v0.2.6
        run: bash tests/smoke-live.sh

  smoke-upstream-latest:
    name: Latest upstream compatibility
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: '0.10.6'
          enable-cache: false

      - name: Run against current upstream tags
        env:
          SPEC_KIT_VERSION: latest
          SPECKIT_GITHUB_ISSUE_CANON_VERSION: latest
        run: bash tests/smoke-live.sh

  workflow-security:
    name: Workflow security
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install actionlint
        env:
          ACTIONLINT_VERSION: 1.7.12
          ACTIONLINT_SHA256: 8aca8db96f1b94770f1b0d72b6dddcb1ebb8123cb3712530b08cc387b349a3d8
        run: |
          archive="actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          curl --fail --location --silent --show-error \
            --output "$RUNNER_TEMP/$archive" \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/$archive"
          echo "$ACTIONLINT_SHA256  $RUNNER_TEMP/$archive" | sha256sum --check --strict
          tar -xzf "$RUNNER_TEMP/$archive" -C "$RUNNER_TEMP" actionlint
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"

      - name: Lint workflow syntax
        run: actionlint

      - name: Install uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990 # v8.3.2
        with:
          version: '0.11.28'
          enable-cache: true
          cache-dependency-glob: ''
          cache-suffix: zizmor-1.27.0
          prune-cache: false
          ignore-nothing-to-cache: true

      - name: Audit workflow security
        run: uvx --from zizmor==1.27.0 zizmor --pedantic .

  required:
    name: CI / required
    if: ${{ always() }}
    needs:
      - static-and-unit
      - smoke-pinned
      - workflow-security
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Require every stable CI lane
        env:
          STATIC_RESULT: ${{ needs.static-and-unit.result }}
          SMOKE_RESULT: ${{ needs.smoke-pinned.result }}
          WORKFLOW_SECURITY_RESULT: ${{ needs.workflow-security.result }}
        run: |
          test "$STATIC_RESULT" = success
          test "$SMOKE_RESULT" = success
          test "$WORKFLOW_SECURITY_RESULT" = success
